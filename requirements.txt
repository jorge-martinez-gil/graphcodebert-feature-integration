# Core (CPU-only) — sufficient for the classical benchmark, tests and reports.
numpy>=1.21
scipy>=1.7
scikit-learn>=1.0
pandas>=1.3
matplotlib>=3.4
pyyaml>=5.4

# Optional neural backend (reproduces the paper; needs a GPU in practice):
#   pip install "featfuse[neural]"
# torch>=1.12
# transformers>=4.20
